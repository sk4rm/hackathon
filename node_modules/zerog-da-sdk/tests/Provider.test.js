const { NHProvider } = require("../lib.commonjs/index.js");

test("Provider", async () => {
    const provider = new NHProvider('http://52.52.127.117:5678');

    const status = await provider.getStatus();
    expect(status).toHaveProperty("logSyncHeight");
});